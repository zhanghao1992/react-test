import React from 'react';

class List extends React.Component {

  constructor(...args) {
    super(...args);
  }

  render() {
    return (
      <div className="Nav">
        {/* {this.state.title} */}
      </div>
    );
  }
}

export default List;
